
from django.templatetags.static import static
from django.conf import settings

dataset =settings.BASE_DIR/'mobile_prices.csv'
import csv
import os

        # path =  "C:\\...." # Set path of new directory here
        # os.chdir(path) # changes the directory
        with open(dataset) as csvfile:
            reader = csv.DictReader(csvfile)
            for row in reader:
                print(row)
                

                p = TrainModel(battery_power=row["battery_power"],bluetooth=row["bluetooth"],clock_speed=row["clock_speed"],dual_sim=row["dual_sim"],front_camera=row["front_camera"],four_g=row["four_g"],internal_memory=row["internal_memory"],mobile_depth=row["mobile_depth"],mobile_weight=row["mobile_weight"],number_cores=row["number_cores"],primary_camera=row["primary_camera"],px_height=row["px_height"],px_width=row["px_width"],ram=row["ram"],screen_height=row["screen_height"],screen_width=row["screen_width"],talk_time=row["talk_time"],three_g=row["three_g"],touch_screen=row["touch_screen"],wifi=row["wifi"],price_range=row['price_range'])
                p.save()
# static('mobile_prices.csv')